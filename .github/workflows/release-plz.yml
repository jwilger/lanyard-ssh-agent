name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  release:
    uses: jwilger/gha-workflows/.github/workflows/rust-release-plz.yml@b4507a0b4110cd0254586382e805832c349e109d
    secrets:
      op-service-account-token: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
